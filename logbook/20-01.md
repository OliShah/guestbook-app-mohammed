## Build Container Images for the Application

### Container 3: Postgres Service

_Current State_
- Pulls base image for postgres from fedora
- Initialises database in postgres on the first run while data dir is empty
- Environment variables are set in Containerfile
- Persistence is applied via VOLUME.  Name the volume at runtime.


### Container 3: Redis Service

_Current State_
- Persistence is applied via VOLUME.  Name the volume at runtime.
- 
